// This file can be used to store shared TypeScript types and interfaces for the frontend.

// Example:
// export interface UserProfile {
//   id: string;
//   name: string;
//   email: string;
// }

// For ExpenseSheet and ExpenseEntry, we can import them directly from "brain/data-contracts"
// e.g. import { ExpenseSheet } from "brain/data-contracts";

// However, if we need to extend them or create view-specific models, this is the place.

export {}; // Ensures this is treated as a module
